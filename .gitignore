# 独立产品各自有仓库，主站只放站点本身
# Az-term → Azi36/Az-terminal-tools
# Az-im  → Azi36/Az-image-tools

# 系统 / 编辑器垃圾
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# 日报产物不进 main：工作流每天生成，存 daily-data 分支，发布时才合回来。
# 本地想预览就 node tools/daily.mjs all，生成的东西留在这儿不会被提交。
daily/

# 顺手跑脚本留下的
node_modules/
*.log
_themetest.html

# 密钥兜底：这个仓库本来就不该有，但别让手滑的一次提交把它带进历史
# （进了历史就得重写历史才能拿掉，而重写是有代价的）
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
id_rsa
id_ed25519
